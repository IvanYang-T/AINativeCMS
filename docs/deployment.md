# 部署指南

## 后端

1. 在 `backend/` 目录运行：
   - `mvn clean package`
   - `java -jar target/backend-0.1.0.jar`

2. 默认端口：8080

## 前端

1. 在 `frontend/` 目录运行：
   - `npm install`
   - `npm run dev`

2. 默认端口：3000
